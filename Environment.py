class Environment:

    def __init__(self,w,h):
        self.width = w
        self.height = h
        self.elements = []
        self.agents = []

    def observationFor(self, agent):

    def atualization(self):

    def takeAction(self,action, agent):
$        # environment.py
        from abc import ABC, abstractmethod
        from typing import List

        
